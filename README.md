Build dev tools
---------------

Build gcc, cmake and clang:

```sh
./build.sh
# or
# ./build.sh <path what you want>
```

Usage:

```sh
source $HOME/env-dev/activate

g++ --version
clang --version
cmake --version
```

